<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.toreytaylor.utacaterering">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".controller.Activites.MainLogin">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".controller.Activites.Registration" />
        <activity android:name=".controller.Activites.CatererHome" />
        <activity android:name=".controller.Activites.CustomerHome" />
        <activity android:name=".controller.Activites.ReservedEvents" />
        <activity android:name=".controller.Activites.PendingEvents" />
        <activity android:name=".controller.Activites.StaffHome" />
        <activity android:name=".controller.Activites.AssignedEvents" />
    </application>

</manifest>